# .understandignore — exclusions for /understand analysis
# Scope: "Full system, noise excluded" — the real ECC surface minus duplicate
# cross-harness mirrors and bulk docs. Built-in defaults already drop
# node_modules/, .git/, dist/, *.lock, lockfiles, images/fonts, *.min.js,
# LICENSE, .vscode/, .idea/, etc.

# --- Cross-harness mirror & tool-config dirs (duplicate copies of the
#     canonical top-level skills/ agents/ commands/ rules/) ---
.claude/
.agents/
.cursor/
.kiro/
.codex/
.opencode/
.gemini/
.qwen/
.zed/
.trae/
.codebuddy/
.codex-plugin/

# --- Bulk documentation (1,494 files; already covered by the component map) ---
docs/

# --- Static assets ---
assets/

# --- Our own analysis output (never analyze the graph dir itself) ---
.understand-anything/
